language: rust
rust:
  - stable
  - nightly

before_script:
  - rustup component add clippy-preview
script:
  - cargo clippy --all-targets --all-features -- --allow clippy::map_entry --deny warnings
  - cargo build --verbose --all
  - cargo test --verbose --all

cache: cargo
